

export const dbConfig = {

    user : "root",
    password : "",
    host : "localhost",
    database : "JWT",
    bcrypt : 10
}

export const serverConfig = {
    port : 3000
}

export const TokenSecret ={
    jwt : "olamundo"
}